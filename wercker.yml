box: debian
build:
  steps:
    - apt-get install git
    - arjen/hugo-build:
        version: "0.14"
deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        domain: blog.devhunter.co
        basedir: build
